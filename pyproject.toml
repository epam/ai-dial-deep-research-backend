[project]
name = "dial-deep-research"
version = "0.0.0"
description = "DIAL-native LangChain tool-calling agent that connects to a DIAL Generic RAG MCP server"
readme = "README.md"
requires-python = ">=3.13,<3.14"
